import React from 'react';
import { Typography } from '@mui/material';

const Contact = () => {
  return (
    <div>
      <Typography variant="h2">Contact Us</Typography>
      <Typography variant="body1">Contact us page content</Typography>
    </div>
  );
};

export default Contact;