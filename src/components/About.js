import React from 'react';
import { Typography } from '@mui/material';

const About = () => {
  return (
    <div>
      <Typography variant="h2">About Us</Typography>
      <Typography variant="body1">This is about us page</Typography>
    </div>
  );
};

export default About;